doctype 5
html(lang="en")
  head
    meta( charset="utf-8")
    title= 'Glisa'
    link(href='http://fonts.googleapis.com/css?family=Flavors', rel='stylesheet', type='text/css')
    link(href="/css/screen.css", type="text/css", rel="Stylesheet")
  body
    .wrapper
      header
        h1 Glisa
      .content
        - if (!everyauth.loggedIn)
          a(href="/auth/google") Connect with Google
        - else
          p Logged In: #{everyauth.google.user.name}